public class Expresiones {
    
    //Indica el tipo de cada una de las siguientes expresiones:
    
    //a) corto*miByte + edad*2/3                    int
    //b) corto*rango + edad*entero                  float
    //c) edad*50L + corto + miByte                  long
    //d) (int) (precio*rango+entero*edad)           int
    //e) edad*23.33 + entero*rango                  double
    //f) corto + miByte + rango + edad + precio     double
    //g) (float) (edad + entero)                    float
    //h) (float) edad + precio                      double
    //i) miByte*23.33F + edad                       float
    
}
